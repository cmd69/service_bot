services:
  subscription_telebot:
    build: .
    container_name: subscription_telebot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ./texts:/app/texts:TEXTS_AUTO_RELOAD
    environment:
      - PYTHONUNBUFFERED=1
      # Configuración de textos
      - CUSTOM_TEXTS_PATH=${CUSTOM_TEXTS_PATH}
      - GENERIC_TEXTS_PATH=${GENERIC_TEXTS_PATH}
      - TEXTS_FALLBACK_TO_GENERIC=${TEXTS_FALLBACK_TO_GENERIC}
      - TEXTS_AUTO_RELOAD=${TEXTS_AUTO_RELOAD}
      - TEXTS_ENCODING=${TEXTS_ENCODING}
      - TEXTS_VERBOSE_LOGGING=${TEXTS_VERBOSE_LOGGING}
      # Configuración de base de datos
      - DATABASE_URL=${DATABASE_URL}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
    depends_on:
      - mysql
    networks:
      - subscription_bot_network

  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init:/docker-entrypoint-initdb.d
    networks:
      - subscription_bot_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: subscription_telebot_phpmyadmin
    restart: unless-stopped
    environment:
      - PMA_HOST=mysql
      - PMA_PORT=3306
      - PMA_USER=${MYSQL_USER}
      - PMA_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    ports:
      - "8080:80"
    depends_on:
      - mysql
    networks:
      - subscription_bot_network

  dbeaver:
    image: dbeaver/cloudbeaver:ea
    container_name: subscription_telebot_dbeaver
    restart: unless-stopped
    ports:
      - "8081:8978"
    environment:
      - JAVA_OPTS=${DBEAVER_JAVA_OPTS}
      # Configuración de administrador
      - CB_ADMIN_NAME=${DBEAVER_ADMIN_NAME}
      - CB_ADMIN_PASSWORD=${DBEAVER_ADMIN_PASSWORD}
      # Configuración de base de datos
      - CB_DATABASE_URL=jdbc:mysql://mysql:3306/${MYSQL_DATABASE}
      - CB_DATABASE_USER=${MYSQL_USER}
      - CB_DATABASE_PASSWORD=${MYSQL_PASSWORD}
      - CB_DATABASE_DRIVER=mysql
      # Configuración adicional
      - CB_SERVER_NAME=JellyBot Database
      - CB_SERVER_URL=http://localhost:8081
    volumes:
      - dbeaver_data:/opt/cloudbeaver/workspace
    depends_on:
      - mysql
    networks:
      - subscription_bot_network


volumes:
  mysql_data:
  dbeaver_data:

networks:
  subscription_bot_network:
    driver: bridge